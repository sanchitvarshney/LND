# Git-based deploys

- **GitHub** holds the source (this repo).
- **Backend** (`server/`) auto-deploys to Plesk at https://trainingapi.mscorpres.net via Plesk's Git tool.
- **Frontend** (`client/`) auto-deploys to Vercel (root directory: `client`).

See PLESK.md and DEPLOY.md for details.
